<!--
Этот блок предназначен для вставки в начало DocumentationUCZONE.md.
Важно: блок не удаляет исходный текст, а только добавляет навигацию и соглашения.
-->

---

## 📌 Как пользоваться этим файлом

Этот файл — **склеенный экспорт** документации UCZONE API v2.0 из GitBook в один большой Markdown.  
Чтобы быстро ориентироваться:

- Начинай с [Starting Guide](#starting-guide), если пишешь первый скрипт.
- Для событий/хуков смотри раздел [Callbacks](#callbacks).
- Для констант/перечислений — [Enums](#enums).
- Для объектов игры (Entity/NPC/Ability/Modifier) — [Game Components - Core Objects](#game-components---core-objects).

### Быстрые ссылки

| Что нужно | Куда идти |
|---|---|
| Запуск скрипта, примеры | [Starting Guide](#starting-guide) |
| Список событий (OnUpdate/OnDraw/OnEntityCreate и т.д.) | [Callbacks](#callbacks) |
| Перечисления (Enum.*) | [Enums](#enums) |
| Рендер, панорама | [Rendering and Visuals](#rendering-and-visuals) |

### Соглашения

- `[...]` рядом с полем (например, **`[?]`**) означает, что значение может отсутствовать (обычно `nil`).
- `(default: nil)` означает значение по умолчанию `nil`.
- В документе встречаются HTML-теги `mark` (наследие GitBook). GitHub их отображает, это ожидаемо.

---
